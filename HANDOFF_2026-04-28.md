# APEX OMEGA, BFR Pipeline Session Handoff

Generated: 2026-04-28
Last commit on branch: de3891c (or later, see git log)
Branch: claude/resume-bfr-pipeline-Uf9Td
GitHub repo: cryptokeysafe/bfrs_to-es

This file is the on/off switch for handing off this project to a new
Claude Code session. Paste the Activation block below as the first
message of the new session. The receiving instance will be fully
briefed in one read with no loss of state.

Apex Omega applies to this document itself. No em dashes, no en
dashes, no markdown asterisk bold, no AI jargon. Plain prose. Cite
source plus section plus date inline. TBD-not-guess.



## Activation block, paste this as message #1 in the new session

```
APEX OMEGA, BFR Pipeline session resume.

Repo:    cryptokeysafe/bfrs_to-es
Branch:  claude/resume-bfr-pipeline-Uf9Td
Resume from commit: de3891c (or the current HEAD on that branch)
Date of handoff:    2026-04-28

Apex Omega methodology is binding. Facts only, no AI jargon, no em
dashes, no en dashes, no markdown asterisk bold, no decorative
Unicode (right arrow, divide, multiply, section sign, ellipsis,
checkmark, curly quotes), three-bucket fact separation,
TBD-not-guess, inline citations with source plus section plus date,
plain prose. The audit/strip_dashes_and_bold.py sweeper enforces the
typography rule and can be re-run any time.

Step 1. Confirm you can see the repository tree at this branch and
that HEAD is at or after de3891c. If not, stop and tell the user;
do not start work in a different tree.

Step 2. Invoke the project-local skill:
    /bfr-pipeline
(equivalent: read .claude/skills/bfr-pipeline/SKILL.md and follow
it).

Step 3. Read these files in this exact order:
    1. APEX_OMEGA.pdf                  (binding methodology)
    2. CLAUDE.md                       (repo overview, Apex Omega
                                        rules, input contract,
                                        operational modes,
                                        definition of done,
                                        terminology rules)
    3. audit/PIPELINE.md               (six-layer pipeline contract,
                                        Formats A/B/C/D, modes,
                                        acceptance test)
    4. audit/BFR_GENERATOR_NOTES.md    (methodology workbook tour:
                                        TFSMS gate, named-range API,
                                        CCN_Library, Okinawa
                                        adjustments)
    5. audit/STYLE_GUIDE.md            (cosmetic spec extracted from
                                        CLB-4 SW; binding for
                                        typography only, structural
                                        patterns derive from FC
                                        2-000-05N)
    6. audit/FINDINGS.md               (round-1 forensic findings on
                                        CLB-4 SW BFR)
    7. audit/CLASSIFICATION_RULES.md   (Layer 3 rule schema and
                                        general-pattern rules; rule
                                        table at
                                        audit/CLASSIFICATION_RULES.yaml)

Step 4. Confirm absorption by stating, in one sentence each:
    (a) what Apex Omega is
    (b) what the six pipeline layers are
    (c) the three operational modes
    (d) the ten acceptance-test items
    (e) current state of CLB-4's BFR (the worked example): how many
        GSF the authoritative SW BFR rolls up to and why that number
        is materially incomplete
    (f) what the eight validator checks are and what the most recent
        run of pipeline/validate.py against out/CLB4_BFR_full.xlsx
        reported

Step 5. Stop and wait. Do not start coding. Do not modify any source
workbook. The next concrete action is for me to either supply the
two Series PDFs that unblock the pattern-ratification work, or to
direct the pipeline at a different unit's TFSMS exports (the pipeline
is unit-agnostic; CLB-4 is one example, not a gold standard).

Hard rules carried over (Apex Omega Sec. 2 + Sec. 6 + Sec. 8):

  Facts only. No assumptions, no speculation, no AI jargon.
  TFSMS-to-ASR reconciliation gate is mandatory before any BFR is
    releasable; never treat TFSMS alone as authoritative.
  Cosmetic fidelity to the CLB-4-extracted typography (NAVFAC
    convention: theme tints, Calibri/Arial fonts, thin black
    borders, banner block on every CCN sheet) plus the Apex Omega
    four-color cell-role palette (FFF8DC input, EAF3F4 calc,
    DCE7C8 output, F8E2D6 warning).
  Math airtight. No restricted-range lookups. No IFERROR top-level
    masking. Zero #REF!, #DIV/0!, #NAME?, #VALUE!, #NULL!, #NUM!,
    #N/A after recalc.
  Terminology: MCIPAC and MCB Camp Butler are correct. MCBJ, MCBB,
    and COMMARCORBASESJAPAN are legacy and not used as place or
    organization terms anywhere.
  Typography: no em dashes, no en dashes, no markdown asterisk
    bold, no decorative Unicode. Use plain ASCII. Hyphens within
    identifiers and compound words (FC 2-000-05N, NAVFAC P-72,
    unit-agnostic) stay because they are part of the identifier.
  CLB-4 is one observed example, not a gold standard. There is no
    gold standard. Patterns derive from FC 2-000-05N, not from
    any single example unit.
  DD 1391 is a downstream MILCON document, not a BFR field. Do not
    add DD 1391 references to BFR documents or workbooks.
  Branch: develop on claude/resume-bfr-pipeline-Uf9Td. Commit and
    push at meaningful milestones.

Acknowledge in plain prose. Lead with the answer. No preamble.
```



## State of play (as of commit de3891c)

### Pipeline layers, status

| Layer | Description | Status |
|---|---|---|
| 1 | Source format awareness, Formats A/B/C/D | Documented in `audit/PIPELINE.md`. Format D ingestion tooling pending real PDF source arrival. |
| 2 | CCN+suffix tagging, vocabulary lock | DONE. 1,059 canonical CCNs from FC 2-000-05N Appendix A in `audit/CCN_VOCABULARY.{yaml,csv,json}`. Source date 2019-06-27. |
| 3 | Classification rules, billet to NOTE-tag function | DONE-starter. `audit/CLASSIFICATION_RULES.{md,yaml}` plus `pipeline/classify.py`. Four general-pattern rules at confidence=high. 14 BMOS-prefix rules at confidence=low pending FC 2-000-05N ratification. Apex Omega rule 4 honored: TBD rules emit unclassified, never a guessed tag. |
| 4 | In-workbook TO/TE schema (Format B) | Documented; populated by `pipeline/etl.py`. |
| 4 | ETL (Formats A/C/D to Format B) | DONE for Format A. `pipeline/etl.py` reads Format-A T/O&E files, classifies, populates TO and TE on the generated BFR. End-to-end run on the four CLB-4 company TFSMS files: 359 billets, 885 equipment lines, 175 classified (49%), 184 surfaced as orphans. |
| 4 | Canonical BFR template generator (Track B) | DONE. `pipeline/template.py` produces a Format-B BFR with the CLB-4 cosmetic banner block on every CCN sheet plus the Apex Omega four-color cell-role palette. |
| 5 | Stable lookup contracts in CCN sheets | DONE. `BFR_Generator_FC2-000-05N.xlsx` has 1,060-entry CCN_Library, full-column lookups, fullCalcOnLoad=True. |
| 5 | Specialized CCN sheet patterns | DONE-starter. Three observed shapes from CLB-4 (primary_items, admin, shop_with_bays) plus default. NOT a canonical Layer 5 set; pending ratification against FC 2-000-05N. |
| 6 | Validation harness | DONE. `pipeline/validate.py`, eight checks: schema, NOTE coverage, NOTE-CCN consistency, vocabulary, cell errors, roll-up integrity, billet accounting, equipment accounting. Unit-agnostic. |

### Decisions ratified by the user, verbatim

  "clb-4 is not the only unit we are working with this tool must be
  agnostic" applies, pipeline is unit-agnostic.
  "this must be done for all units CLB_4 is just an example" applies.
  "those data pipelines have to be able to take that information in
  the format that it's in and put it into the BFRL regardless of the
  way that it's received" applies, Excel and PDF inputs both required.
  "It's not necessarily the gold standard, but look at how it's put
  together" re: CLB-4 SW BFR, CLB-4 is an example, not the gold
  standard, and there is no gold standard. Cosmetic typography from
  CLB-4 is binding because it is the NAVFAC convention used across
  DoD BFRs; row-by-row structural patterns are not.
  "It needs to mathematically all the complications and formulas need
  to be baked and everything needs to be perfect" applies, math
  airtight, zero formula errors.
  "MCBJ that's old-school, that heading is technically incorrect"
  applies, MCIPAC and MCB Camp Butler nomenclature, MCBJ and
  COMMARCORBASESJAPAN are banned as place or organization terms.
  "there are no DD1391's with BFRs" applies, DD 1391 is a downstream
  MILCON document, removed from all BFR docs and the methodology
  workbook.
  "NO DASHES NO EM DASHES NO double-asterisk NONE OF THAT CRAP"
  applies, typography rule encoded as CLAUDE.md rule 9 and enforced
  by `audit/strip_dashes_and_bold.py`.
  "the book must have the capability to add more CCNs and
  versatility for different units" applies, CCN_Library expanded to
  1,060 entries, generator supports any number of CCNs and any
  unit type via the profile + ccn-list inputs.

### Commit chain on `claude/resume-bfr-pipeline-Uf9Td`

  c328a36  Layer 2 vocabulary lock (1,059 CCNs)
  0a2c8cc  Drop DD 1391 plus MCBJ purge
  1a61265  Workbook rename MCBJ_BFR_Generator to BFR_Generator
  2b87388  Skill update, round 4 state
  0817a1c  Layer 5 CCN_Library to 1,060 entries
  5f79056  Skill update, Layer 5 done
  95ea12a  Track C, Layer 6 validator (six checks)
  120f786  Skill update, Track C done
  adcf5c9  Typography sweep (em dashes, en dashes, ** bold)
  6369ff6  Track B, Layer 4 BFR template generator
  601f1cb  Skill update, Track B done
  25834e1  Skill, pipeline package and tooling refresh
  28a02f9  Extended Unicode sweep (arrow, divide, multiply, etc.)
  b9ab701  Layer 5 specialization, three pattern templates
  15bd3b5  Reframe CLB-4 as one observed example, not gold standard
  bd17135  Skill, six-step ratification path
  379b4a2  Layer 6 advanced checks plus planning-factors scaffold
  16caec1  Layer 3 classification rules starter
  de3891c  Items 5 and 6, classifier plus end-to-end ETL

### What's left, in order

BLOCKED on user-supplied files (sandbox cannot reach wbdg.org).

  1. Drop FC 2-000-05N Series 100
     (`fc_2_000_05n_100series_12_10_2025.pdf`, version 100.20251210,
     10 Dec 2025) and Series 200
     (`fc_2_000_05n_200series_05_16_2025.pdf`, version 200.20250516,
     16 May 2025) into a commit on `main`.
  2. Run `audit/extract_planning_factors.py` (already scaffolded).
  3. Ratify Layer 5 pattern shapes against the extracted factor table.
  4. Ratify Layer 3 BMOS rules in `audit/CLASSIFICATION_RULES.yaml`.

NOT BLOCKED, can start anytime.

  5. Author `audit/TAMCN_CCN_MAP.yaml`, the TAMCN-to-facility-CCN
     doctrine table. Closes Check 8 of the validator (885 TE
     orphans currently).
  6. Author `audit/UNIT_TYPE_DEFAULTS.yaml`, per-unit-type
     admin_ccn defaults so the classifier picks the right admin CCN
     for aviation squadrons, MEU, depot, schoolhouse, recruit
     depot, etc.
  7. Track D PDF ingestion prototype (`pipeline/pdf_ingest.py`).
     Builds against a real TFSMS or ASR PDF when one arrives.

### Open questions for the user

  1. Series 100 and Series 200 PDFs. Drop them into a commit on
     `main` so the pipeline can pull them in via the GitHub MCP.
  2. Per-unit-type admin_ccn defaults. The classifier currently
     defaults to 61072 (USMC BN/squadron HQ). Aviation, MEU, depot,
     etc. need their own defaults. SME input.
  3. TAMCN to facility CCN doctrine. Source: FC 2-000-05N TAMCN
     dimensional table or unit-doctrine input.

### Next concrete action

User to drop Series 100 and Series 200 PDFs into a commit on `main`,
or direct the pipeline at a different unit's TFSMS exports. The
pipeline is unit-agnostic; CLB-4 is the worked example.

End of handoff.
